DROP TABLE IF EXISTS MEMBER CASCADE;
CREATE TABLE MEMBER
(
--  BIGINT는 자바에서의 LONG TYPE이다.
--  GENERATED BY DEFAULT AS IDENTITY는 NULL 값 입력 시 자동으로 값을 채워준다.
	ID BIGINT GENERATED BY DEFAULT AS IDENTITY,
	NAME VARCHAR(255),
	PRIMARY KEY(ID)
);